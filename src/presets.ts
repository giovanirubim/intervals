export enum Cursor {
	Default = 'default',
	Grab = 'grab',
	Grabbing = 'grabbing',
	ColResize = 'col-resize',
}

export enum Color {
	Block = '#eee',
	BlockEndLine = '#444',
}

export const minMouseDist = 3
export const minMouseMove = 2
